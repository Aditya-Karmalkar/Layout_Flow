name: layout_flow_example
description: Example app demonstrating layout_flow.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  layout_flow:
    path: ../

flutter:
  uses-material-design: true
